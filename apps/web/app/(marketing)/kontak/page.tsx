import type { Metadata } from 'next';

import { DocStub } from '../_components/DocStub';

export const metadata: Metadata = { title: 'Hubungi kami — AI Body Coach' };

export default function KontakPage() {
  return (
    <DocStub
      title="Hubungi kami"
      body="Kanal dukungan resmi sedang disiapkan bersama nomor WhatsApp Business. Halaman ini akan berisi cara menghubungi kami begitu nomornya terverifikasi."
    />
  );
}
