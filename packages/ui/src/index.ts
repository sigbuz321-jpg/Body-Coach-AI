/**
 * @bodycoach/ui — design system "Piring & Plat" (docs/03-design-system.md).
 *
 * Semua komponen tinggal di sini, bukan di dalam folder halaman apps/web.
 * Komponen memakai token dari tokens.css, bukan nilai hardcode dari file desain.
 *
 * Isi menyusul di M3: tokens.css, Button, GoalCard, Stepper, Slider, Chip,
 * ProgressBar, PlanCard, MacroBar. M8: PlateStack, WeightChart, StatTile,
 * FoodLogItem, EmptyState.
 */

export const UI_PACKAGE = '@bodycoach/ui' as const;
